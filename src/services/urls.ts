/*Urls for the application */
export const prefixer = 'https://api.covid19api.com/';

export const authUrls = {
	//Auth URI
	SIGNINUSER: `${prefixer}`,
	LOGINUSER: `${prefixer}authentication/login`,
	SUMMARY: `${prefixer}summary`,
};

